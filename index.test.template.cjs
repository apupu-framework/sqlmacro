const { sqlmacro, plainsql, DELETE_RIGHT_COMMA, DELETE_LEFT_COMMA } = require("sqlmacro")
const { test, describe, it, before, after }  = require( 'node:test' );
const assert = require( 'node:assert/strict' );

${BODY}

